server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        # Day 5 task — uncomment and wire up once auth-service/expense-service/notification-service exist:
        # - id: auth-service
        #   uri: lb://auth-service
        #   predicates:
        #     - Path=/api/auth/**
        # - id: expense-service
        #   uri: lb://expense-service
        #   predicates:
        #     - Path=/api/expenses/**
        # - id: notification-service
        #   uri: lb://notification-service
        #   predicates:
        #     - Path=/api/notifications/**
        # Docs routes — uncomment together with the routes above so the aggregated
        # Swagger UI below can fetch each service's /v3/api-docs through the gateway:
        # - id: auth-service-docs
        #   uri: lb://auth-service
        #   predicates:
        #     - Path=/api/auth/v3/api-docs
        #   filters:
        #     - RewritePath=/api/auth/v3/api-docs, /v3/api-docs
        # - id: expense-service-docs
        #   uri: lb://expense-service
        #   predicates:
        #     - Path=/api/expenses/v3/api-docs
        #   filters:
        #     - RewritePath=/api/expenses/v3/api-docs, /v3/api-docs
        # - id: notification-service-docs
        #   uri: lb://notification-service
        #   predicates:
        #     - Path=/api/notifications/v3/api-docs
        #   filters:
        #     - RewritePath=/api/notifications/v3/api-docs, /v3/api-docs

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}

jwt:
  secret: ${JWT_SECRET:change-me-in-.env}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    # Uncomment together with the *-docs routes above (Day 5) for one aggregated
    # Swagger UI covering every service, served at http://localhost:8080/swagger-ui.html:
    # urls:
    #   - name: auth-service
    #     url: /api/auth/v3/api-docs
    #   - name: expense-service
    #     url: /api/expenses/v3/api-docs
    #   - name: notification-service
    #     url: /api/notifications/v3/api-docs

management:
  endpoints:
    web:
      exposure:
        include: health
